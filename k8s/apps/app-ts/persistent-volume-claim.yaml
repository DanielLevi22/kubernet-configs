apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: first-pvc

spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: standard

  selector:
    matchLabels:
      name: first-pv