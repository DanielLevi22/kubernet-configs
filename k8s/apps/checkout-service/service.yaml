apiVersion: v1
kind: Service

metadata:
  name: checkout-service
  namespace: marketplace

spec:
  type: ClusterIP
  selector:
    api: checkout-service
  ports:
    - port: 80
      targetPort: 3003
