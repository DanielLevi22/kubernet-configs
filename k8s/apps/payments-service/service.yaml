apiVersion: v1
kind: Service

metadata:
  name: payments-service
  namespace: marketplace

spec:
  type: ClusterIP
  selector:
    api: payments-service
  ports:
    - port: 80
      targetPort: 3004
