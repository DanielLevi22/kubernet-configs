apiVersion: v1
kind: ConfigMap

metadata:
  name: users-service
  namespace: marketplace
data:
  PORT: "3000"
  NODE_ENV: production
  DB_HOST: host.docker.internal
  DB_PORT: "5433"
  DB_USERNAME: postgres
  DB_DATABASE: users_db
